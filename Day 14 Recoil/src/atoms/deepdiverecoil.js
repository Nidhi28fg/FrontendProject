import { atom} from "recoil";

export const myNetworkAtom = atom({
        key: "myNetworkkAtom",
        default: 102,
});

export const jobsAtom = atom({
        key: "jobssAtom",
        default: 0,
});

export const messagingAtom = atom({
    key: "messaginggAtom",
    default: 12,
});

export const notificationAtom = atom({
    key: "notificationnAtom",
    default: 0,
});